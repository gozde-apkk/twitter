


export default function Header ({title}){
    return (
        <header className="sticky top-0">
            header
        </header>
    )
}

Header.propTypes = {
    title : PropTypes.string.isRequired
}